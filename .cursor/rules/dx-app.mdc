---
description: DEEPX dx_app standalone inference application framework
alwaysApply: true
---

**Response Language**: Match your response language to the user's prompt language — when asking questions or responding, use the same language the user is using.

# dx_app — Cursor Rules

## Overview
dx_app provides 133 models across 15 AI tasks with Python (4 variants) and C++ examples.

## Context Routing
| Task | Read |
|---|---|
| Python app | `.deepx/skills/dx-build-python-app.md` |
| C++ app | `.deepx/skills/dx-build-cpp-app.md` |
| Async/performance | `.deepx/skills/dx-build-async-app.md` |
| Model management | `.deepx/skills/dx-model-management.md` |
| Validation | `.deepx/skills/dx-validate.md` |
| Always | `.deepx/memory/common_pitfalls.md` |

## Critical Conventions
1. IFactory pattern — 5 methods mandatory
2. `parse_common_args()` — no custom argparse
3. No hardcoded .dxnn paths — CLI args or registry
4. 4-variant naming convention
5. `logging.getLogger(__name__)` — no `print()`
6. C++14 only, RAII with std::unique_ptr
7. `dxrt-cli -s` before NPU operations
8. **Registry key ≠ Python class** — `yolov26` → `YOLOv8Postprocessor` (NOT Yolo26Postprocessor). See `.deepx/skills/dx-build-python-app.md` Step 5
9. **Search existing examples first** — before generating new code, check `src/python_example/<task>/<model>/`
10. **Output accuracy validation** — after smoke test, verify detection count > 0. See `.deepx/skills/dx-validate.md` Level 5

## Interactive Workflow (MUST FOLLOW)
- Ask 2-3 questions before building
- Present plan, get approval
- Validate after generation
- Output to `dx-agentic-dev/<session_id>/` by default

### Mandatory Questions for App Building (HARD-GATE)

<HARD-GATE>
Before ANY code generation, the agent MUST ask and receive explicit answers to ALL 3:
1. **Language/variant**: Python (sync/async/cpp_postprocess/async_cpp_postprocess) or C++?
2. **AI task**: detection, classification, segmentation, pose, face_detection, depth_estimation, etc.
3. **Model**: Specific model name (e.g. 'yolo26n') or 'recommend' for auto-selection

These questions MUST NOT be skipped even if the user's prompt seems to provide enough context.
Even "build a yolo26n detection app" requires confirming: Python or C++? Which variant?
</HARD-GATE>

## Process Skills (available for all tasks)
| Skill | When to use | Read |
|---|---|---|
| Brainstorm & Plan | Before any code generation | `.deepx/skills/dx-brainstorm-and-plan.md` |
| TDD | Validate each file after creation | `.deepx/skills/dx-tdd.md` |
| Verify Completion | Before claiming task is done | `.deepx/skills/dx-verify-completion.md` |

## Hardware
| Architecture | Value |
|---|---|
| DX-M1 | `dx_m1` |
| DX-M1A | `dx_m1a` |

## Session Sentinels (MANDATORY for Automated Testing)

When processing a user prompt, output these exact markers:
- **First line of response**: `[DX-AGENTIC-DEV: START]`
- **Last line after ALL work complete**: `[DX-AGENTIC-DEV: DONE]`
- Sub-agents invoked via routing do NOT output sentinels
